package com.yourname.demo.model.enums.db;
import java.util.Map;
import java.util.HashMap;
import com.felees.hbnpojogen.persistence.impl.StringValuedEnum;
/**
 * Defines the CustomerTransactionDrcrEnum enumeration.
 * 
 * @author autogenerated
 *
 */
public enum CustomerTransactionDrcrEnum  implements StringValuedEnum {

	/** Enum Constant. */
	DR("DR"),
	/** Enum Constant. */
	CR("CR");
	/**
	 * Internal storage of status field value, see the Enum spec for
 	 * clarification.
 	 */
	private final String status;
	
	/**
	 * Enum constructor for ActiveState.
	 * @param state Value.
	 */
	CustomerTransactionDrcrEnum(final String state) {
		this.status = state;
	}
	
	/** Construct a map for reverse lookup. */
	private static Map<String, CustomerTransactionDrcrEnum> valueMap = new HashMap<String, CustomerTransactionDrcrEnum>();

    static {
    	// construct hashmap for later possible use.
        for (CustomerTransactionDrcrEnum unit : values()) {
            valueMap.put(unit.getValue(), unit);
        }
    }
    
	/**
	 * Current string value stored in the enum.
	 * 
	 * @return string value.
	 */
	public String getValue() {
		return this.status;
	}

	/**
     * Perform a reverse lookup (given a value, obtain the enum).
     * 
     * @param value to search
     * @return Enum object.
     */
    public static CustomerTransactionDrcrEnum lookupEnum(String value) {
        return CustomerTransactionDrcrEnum.valueMap.get(value);
    }
}
